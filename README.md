# One-Click Local Development Setup — Student REST API

This project provides a one-click developer setup using:

- Docker
- Docker Compose
- GNU Make
- Spring Boot
- MySQL
- Flyway migrations

---

## Prerequisites

Install:

- Docker
- Docker Compose (v2+)
- GNU Make
- Java 17+
- Maven

Check installation:

```bash
docker --version
docker compose version
make --version
mvn -version
java -version
